# @package _global_

defaults:
  - gta5_cityscapes_cosine

datamodule:
  superpixel: True

ssl:
  enable: True
  queue_size: 1250000 # 2500 * 500
  classwise_sample: False
  continual_clustering: False
  sample_ratio: 0.1
  unready_update: False

  k: [19, 38]
  k_rep: [2, 1]
  phi: 0.05
