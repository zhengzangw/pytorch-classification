# @package _global_

defaults:
  - override /trainer: test
  - override /module: toy
  - override /datamodule: mnist
  - override /callbacks: none

seed: 12345

datamodule:
  effective_batch_size: 128
  batch_size: 128
  num_workers: 4
  pin_memory: True
