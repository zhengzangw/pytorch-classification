# @package _global_

defaults:
  - imagenet_mixerb
  - override /logger: comet

scheduler:
  warmup:
    times: 10000

trainer:
  batch_size: 128
  gpus: 4
  accumulate_grad_batches: 8
  weights_save_path: /global/cfs/cdirs/m3691/zangwei/pytorch-classification/job_3
